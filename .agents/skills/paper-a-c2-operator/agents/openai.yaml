interface:
  display_name: "Paper A C2 Operator"
  short_description: "审计并推进 Paper A C2 数据、Block 与分发流程"
  default_prompt: "Use $paper-a-c2-operator to audit the current Paper A C2 state and identify the next safe action."

policy:
  allow_implicit_invocation: true
