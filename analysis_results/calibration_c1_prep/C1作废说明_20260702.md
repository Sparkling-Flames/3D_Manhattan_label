# C1 作废说明（2026-07-02）

当前 `analysis_results/calibration_c1_prep/` 下的 C1 启动工件已作废，不得用于启动 C1。

作废范围：

- `calibration_round_input_manifest_v1.json` 已标记为 `deprecated_do_not_launch`
- `c1_launch_readiness_summary.json` 已改为 `passed: false`
- 既有 `assignment_manifest_C1.csv`、project mapping、worker-facing distribution 只保留为旧记录，不作为分发真源

原因：

旧 C1 pool 不再作为正式启动版本。后续目标将改为构造 proxy-balanced Calibration pool，而不是声称严格 scene-stratified。

本次未执行的内容：

- 未重新选择 100 张 Calibration_manual 图片
- 未重新生成 C1_anchor / C1_core / C2_reserve
- 未重新生成 assignment manifest、project mapping 或 worker-facing distribution

边界：

- P1 final closeout v2 不变
- C1 禁止使用 reserve 的边界不变
- C1 仍只形成 provisional worker/task statistics
- 正式 scene candidates / routing / OOD 参数仍需 C1 后形成候选，并在 C2 后冻结
