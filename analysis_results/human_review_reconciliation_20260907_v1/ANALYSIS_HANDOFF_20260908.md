# 交给“梳理研究方向与相关数据 (3)”的当前任务

用户于2026-09-08授权本次接手分析。目标任务：`01a07a38-aeb2-7432-b9c8-a22559e1f9a5`，已核实与当前任务共享本地`D:/Work/HOHONET`。请更新旧上下文：不是重新选择50图或重做问卷，也不要求用户再确认全部评论。

## 当前完成与未完成

人工问卷及评语整理已完成：50条JSON/CSV一致，4条后续补充原样保存，人工立即待办为0。交接检查确认50张高清原图与50张对照图实际在本地。5项定向测试通过。读取最新资料不依赖GitHub；本包及其他变更仍有未提交内容，不声称已上传。

**仍有15张的来源／显示证据事项。** 它们不是15张都不能使用，而是某些结论暂受限制；详细任务在`HANDOFF_PREFLIGHT_20260908.json`和`visual_followup_notes.json`的`next_action`中。接手后先收口这些事项：能从本地证据解决的自行解决；无法确认的保留缺口，对依赖它的分析停止下结论，继续不依赖它的分析。不要把交接就绪说成全部证据问题已解决。

## 阅读顺序与优先级

1. 本文件与`HANDOFF_PREFLIGHT_20260908.json`。
2. `human_supplements_20260908.json`：V04、V15、V27、V48为用户最新明确补充，优先于旧问卷选项和旧AI解释。
3. `reconciled_records.json`：`human_record`是原始人工记录，`scope_reading`是AI提取、`ai_followup`是AI观察，`human_supplement`是最新人工补充，不能混为同等真值。
4. `范围解释整理说明.md`、`人工下一步清单.md`、`README_ZH.md`，注意旧章节中“暂不交接／等待回答／13张优先”是历史过程，已被当前授权、补充与`next_action`取代。
5. `proposal_geometry_checks.json`、`historic_display_mapping_checks.json`以及相邻审图包中的`ORDER_AUDIT.json`。数字检查与视觉解读分别使用。
6. 研究输入底座`analysis_results/uncertainty_cloud_inputs_20260906_v1/`；现有分析与勘误在`analysis_results/uncertainty_visual_review_20260907_v1/`；接收原包在`analysis_results/uncertainty_handoff_received_20260907_v1/`。请核对现有文件后复用，不宣称从原导出重建了所有数据。
7. 导师原交流`C:/Users/ASUS/Desktop/与导师交流.txt`。正文明确对话、用户语音回忆和后加文献索引性质不同。不能把记忆中的模拟／Bi路线当作正式实验决策。

## 四条最新人工补充的含义

- V15：中央门边是墙拐角还是连续墙无法判断；用户认为拼接扭曲且尝试3D无法正交。保留不确定，拼接因果尚未独立验证，3D失败不证明原标注错误或整图OOS。
- V27：纳入右侧内部空间会形成分层天花，仍不知道怎样标合适。OOS判断需带纳入范围条件，不自动排除。
- V48：接缝玻璃门外扩展是用户原本期待的模型行为；用户明确本图按任务规则不应跨门。不得再引用旧multiple_candidates证明用户认可两个合规范围。此判断不推广成曼哈顿世界普遍禁止跨门。
- V04：左右上方天花下凸，extended需忽略，enclosed在此截止；GT和两位工人属于后者。输出差异、近似与规则取舍分开。

## 已发现的读取风险，必须先处理

导出关键点列表排列不等于作者的墙邻接。HD读取按相邻两点组对，历史脚本默认x排序并在5%图宽阈值内配对，还可能有本地覆盖。原导出没有relation边可直接证明墙邻接。失败预览的偶数／奇数红线是脚本生成，不能认定工人自交。

12份旧建议的地面环可计算，10份与历史默认映射相同；这不证明原作者顺序或完整3D正确。已复算V03重复应用排序会再次自交。V06端点角色异常和V10非相邻配对问题不同。任何语义、质量、距离或簇解释都应检查是否受这类读取假设影响。不要为规整3D自动调整原始坐标，不因新读取改变就覆盖旧簇。

## 研究分析任务

完成必要的来源核对后，围绕导师的“不确定性、场景条件、分布／少数解释、有限人员下的稳定与瓶颈”独立分析：

1. 给出证据状态表：已解决、尚未解决、影响哪些结论。改后GT、多视角、worker002与显示簇编号能连接时连接，不能猜测。已有评论明确处不重复问用户。
2. 检查已有几何距离的读取和可计算集合；必要时做明确命名的敏感性对照，不把读取失败直接当工人低质量。先诊断再复用统计。
3. 复用已有真人分簇，连接用户所述范围解释到明确的显示响应／具体版本。允许enclosed内部多种截断、extended程度差异、多簇以及无法判断；不能按Bi模型头名给簇命名。
4. 检查Bi双头差异与真人分歧及已有簇结构的关系，优先连续对照和反例。候选20图没有真人响应，不混入真人分歧样本量。50图是探索性选择，不估计总体歧义发生率。
5. 分开范围类型、局部几何质量、结构可辨识性、规则适用／近似条件及渲染问题。特别保留双头相近但有未解释结构、双头不同但只有一个规则偏好、扩展不足和同类内部分歧。
6. 按building、Manual／Semi、自然／合成初始化及相关人员组成检查。历史人员不按旧退出／资格规则一刀切；后续最多20人的约束与历史数据利用分开。旧42图和扩展73单元等资料保留版本含义与身份去重。
7. 将“有限面板下结果稳定”“真值质量改善”“恢复少数结构解释”分开。不能由15–20人的平坦曲线断言无限招募的不可约质量上限。疲劳机制与Bi两倾向路线独立，不凭当前案例建立对应。
8. 输出客观中文报告：目前值得向导师汇报的发现、支持证据、反例、适用范围、不能下的结论及下一步低成本验证建议。不要代定论文提纲、实验方案或停止人数。

## 交付方式

在新的独立分析结果目录落盘，提供入口报告和必要可复算数据；遵循仓库AGENTS。不要覆盖原人工判断、导出、旧审图包或其他任务正在修改的Panorama Studio。需要改代码时只做有证据支持的必要修改并测试。本次授权是继续分析，不要求额外发布／推送或再委派其他任务。

请先回报本地入口可读、最新补充已识别、开放证据任务已登记，然后持续完成分析；不必等当前任务再次催促。若只能完成部分，明确列出剩余依赖，不能把计划当完成。用户后续在目标任务查看和讨论结果。
