# Pro提示词：先研究图片分类、特质与已有难易标签

以下整段可复制。当前先做图片侧研究；原A—E中涉及真人结果和人员组合的部分保留后续研究身份。

```text
请独立深入研究HOHONET的图片分类、图片特质/模型表征，与用户已填写难易度tag之间的联系。真人标注尚未完全返回，这一轮先以现有难易tag为分析目标，不等待真人结果，不把任务转成收敛判据研究。长期主线仍是人类标注不确定性及人数增长后的收敛；未来再检验本轮图片规律与实际真人结果的联系。

使用codex/image-portrait-20260914分支的最新可用analysis_results/image_portrait_20260914_v1/。先核对AGENTS.md、README、输入版本和实际文件。用户已确认这批标签：648图分类中，简单49、中等41、困难16，共106张有效标签；另6张未定、536张没有该版选用记录。有标签图来自13个building、21个展示组。原字段在metadata/spatial_history.jsonl.gz的latest_selection_record.difficulty；若difficulty_tags_20260915_v1/image_tags.jsonl已可用，也可读取这个衍生表并与原字段对照。衍生目录暂未上传不等于原标签缺失，不补造标签。不要因旧Pro工作流固定提交而漏读后来已补齐的DINO；无法取得的输入明确列出，独立可做的部分继续。

只有labelled图用于难易标签的监督评价；648图可用于注明范围的表征描述。标签是用户判断，不是已验证的真实难度、质量或收敛状态。保留原标签，不按模型效果改标签、改类或删除反例。简单/中等/困难有顺序，但不默认等距；分类和序数结果分别解释。当前使用全部106张已填标签，不额外按是否采用/是否有真人响应筛图。

请自由选择有根据的方法，至少实际回答以下问题，给出支持与不支持的关系，不只写计划：

1. 图片类别与难易tag的关系是什么？先查覆盖和共现。本地计数发现16张困难均为卫浴，卫浴另6张中等；卧室23简单/10中等，起居与休闲17张均中等。先复核这些计数，并分清类别、building和展示组的影响，不直接外推到648图总体。

2. 在类别之外，哪些图片可见特质有信息？用来源明确的边界可见性、遮挡、反射/玻璃、连通与门洞等字段；人工、AI、未知分开。现有AI特质是粗筛，部分字段近常量，不能把特质不准确造成的弱结果解释成图片特质无关。检查同类别内部的对照，以及标签与特质不一致的实例。

3. 哪些模型/层/汇聚刻画了与tag有关的图片差异？比较既有HoHoNet各阶段及旧均值、Bi Fc和两个Fg/双头反馈、uLayout、DINO block3/6/9/11/12 patch及最后CLS、全景/六面、局部/全局、DA3已导出的单图表征与有效数值反馈。预指定候选都报覆盖和结果，另允许有理由的后续探索，但不要只报最佳层。重点检验加入模型信息是否超过仅场景类别基线。DA3联合相机存在已知不一致，不能当可靠物理重建；Bi共享静态embedding不是逐图特征，模型输出不能当tag真值。

4. 图片之间的相似关系与难易是否一致？既看同标签近邻，也保留视觉/模型相似却tag不同、外观不同却同tag的反例。同类跨房、同房视角分别检查。G002/G178/G179/G184/G202这5个展示组内部有不同难易标签，适合提出具体对照，但须根据既有支持关系核对可比较成员，展示组不是物理同房的自动判定。重点回答同一类别里为什么有的图标简单、有的图标中等/困难。

至少保留总体标签基线、仅类别、仅可见特质、仅模型表征，以及类别加入特质/模型的同覆盖比较。简单近邻、线性/正则化方法可以作为起点，允许自主扩展；只有106张标签，不训练视觉网络，不以复杂模型代替有效对照。训练侧完成标准化、PCA、选层和调参，不能用外层标签调出好看的结果。使用父工作包已有留楼/留房关系规则，报告每折标签覆盖和缺类情况；同房视点随机分到两侧的高分不能当泛化证据。报告按图和按楼/支持组件汇总的结果、各类表现及不确定范围。任意新增划分另标探索，不能挑成功划分。

标签及其同义难度文字、历史真人质量/时间/分歧、计划人数、采用决定、组编号和图像序号不得作为标签预测输入。语义类别允许作为明确基线；看过106标签后的探索须注明，不能追记为事前验证。同一全景的旋转和六面不增加独立样本；未标图不伪装成容易，也不把伪标签算真实标签。

只读数值、几何和文字，不查看原图、不下载权重、不调用视觉模型。需要看图的事项交付image_id/配对成员、具体疑问和触发依据，本地再核查。当前不用求解人员分型/ABCD/稳定人数来证明任务完成；这些在后续真人验证计划里说明接口即可。

输出新的cloud/difficulty_tags_20260915_v1/，代码和必要测试放仓库对应image_portrait目录，不改旧Pro输出或正式合同。交付中文报告、逐图标签/预测及折外结果、类别内对照、各层和组合的同覆盖比较、缺失/失败表、来源参数、可运行命令和本地审图队列。输入不足时交代具体少了什么和影响哪些结论，不虚报全部完成。

最终回答：目前已有标签支持哪些图片联系？模型究竟只识别了类别/建筑背景，还是捕捉到类别内部的难易差异？哪些联系尚无支持？最值得本地核查的图片是什么？真人数据返回后，怎样检验这些判断与实际不确定性及收敛的关系？当前结论不以预测成本、质量上限或收敛状态为替代目标。
```
