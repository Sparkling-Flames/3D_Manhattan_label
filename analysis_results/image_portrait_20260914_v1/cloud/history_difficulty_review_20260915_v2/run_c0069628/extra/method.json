{
  "subsetting": "200 x 80% actual distinct people without replacement; conditional on cluster retaining >=2; report retention separately",
  "added_point_sensitivity": "exclude only the two explicitly imputed confirmed added records; keep removed-point corrections; never reinterpret them as fabricated",
  "neither_resampling_nor_cuts_are_new_samples": true,
  "model_prediction_not_refit": true
}
