# 可复制给Pro：图片特质与历史难度候选的联系

请在HOHONET的codex/image-portrait-20260914分支继续实际研究。先读AGENTS.md、图片工作包PRO_START_HERE.md及review_workflow_20260915/README.md，再读取两轮原始报告、方法和逐图结果。只使用已取得的数值、几何、文字，原图及视觉判断由本地完成，不下载权重、不调用视觉模型。

用户确认早期k=2—8（含8），两轮新增研究由用户授权；旧k<8文本已经过时。用户选择先推进图片联系，同时明确粗分类后续由本人审核。算法类别是可修订工作候选，审核前可以分析，但不能称人工确认真值；审核后重算受影响部分。未审核不等于认可，暂缓不等于困难。

目标：研究图片自身的信息条件、空间组织与历史标注难度、不确定性及随真实人数增长的稳定过程之间的联系。最终不只交阈值讨论或模型排行榜。模式发现、重复支持、成员复现、支持核心稳定、完整分布稳定及质量分别记录，允许稳定多簇。

一、接续输入，不从零重复。

优先处理接收方发现的覆盖问题：review_workflow_20260915/oos_geometry_coverage_audit.json逐份确认9张旧oos任务、216份响应均为无辅助且unassisted_manual_included=true，带几何点集。禁止仅按raw_condition=oos称其纯规则任务并略去几何。根据实际资格/范围记录单列其几何及过程分析，与旧205图口径并报，不强行混成in-scope。完整历史214图与106tag的并集为285图；当前审核工具276图未含这9图，不能把工具覆盖当全历史覆盖。

第一轮在cloud/history_difficulty_20260915_v1/run_13859d59，已完成A—E；第二轮在cloud/history_difficulty_review_20260915_v2/run_c0069628，已重做分类诊断，尚未按新类重拟合A—E。保留第一轮展示k=7成绩的原名，不直接改成k≤8。第二轮final_review_draft_per_image和USER_DECISION_TABLE是候选依据，原文件保持；新分析输出cloud/image_links_after_review_20260915_v1/。未取得数组或记录要列具体缺项。

以image_id、condition关联230图×条件；205张历史图片与106张人工tag重叠35张，并集276张。648图是画像覆盖，不是648张有真人目标。核对逐份来源与OOS纳入语义：旧196张无辅助与187 Manual＋9 OOS任务必须按实际作答和明确资格解释，不能只凭任务名改变资格。W011历史保留，W019/W026排除主分析，Manual/Semi分开，人工补删点沿用，不新增自动修复。

旧实验自带difficulty及同义重编码停用；106张用户tag保留单独对照，不拿它挑历史分类阈值，不把两套标签合并。用户审核JSON如果已经提供，使用status=已审核的独立值，保留其选择原因；未提供就只报告候选结果。审核理由不进入纯图片预测特征。

二、A—E全部给出结果或明确覆盖缺口。

A：以场景类别／主空间为基线，比较同类内部差异。检验边界可见性、遮挡位置、空间连接、局部结构和反射与不同过程量的联系。旧墙地边界与遮挡是完全重合的初筛字段，不当两份独立证据；连接近常量、反射unknown受分辨率影响，不能据弱结果宣布因素无关。新增本地描述若未提供，先分析现有可靠记录、列出所需字段；不得编造新目视结论。

B：比较布局点数、Bi双头范围差异、旋转及跨模型差异。对“模型分歧低/高×真人分歧低/高”给出具体例子。区分候选范围、定位变化、点数及模型失败，不把模型一致当GT。

C：复用已导出的HoHoNet、Bi、uLayout、DINO、DA3层与汇聚候选，在类别与简单反馈基线之上检验增量。全部固定候选与训练内选层都报告；旧逐通道标准化与新L2核方案分开，不把预处理变化解释成模型进步。不重跑视觉推理。

D：同房不同视角、同类不同房间和跨楼分别比较。纯图片预测与允许其他来源图历史的条件预测分开。保留实际邻居ID、房间关系及覆盖，未决关系不泄漏；旧同房结果的负增量同样保留。候选标签配对不足就报告连续过程及缺口，不凑同房准确率。

E：检查图片特质与人员构成的交互。比较同图相同人数的真实不同名单，及共同名单的跨图差异。人员特征在目标房间/楼之外形成；保留质量、时间、规则、Semi的既有15种信息组合及质量＋时间＋修改幅度方案，连续特征和不分型作基线。先登记各方案实际可用覆盖，不将缺数据者硬分一类；在可形成的同图真实不同人员上分析AA、AB、AAB、AABC、ABCD等，不复制人员、不强制四类或均分。

三、有限而有意义的比较。

使用第一轮类、第二轮候选类、审核类（若有）分别检验，并保留连续过程量，类别规则不按预测分数选择。第二轮中等结构候选仍用原名，不能自动说完整分布已经稳定；困难限于观察内。审图导致的标签修改只记录明确改变及理由，不静默改聚类。

每图用实际人数；共同人数及共同名单仅作补充检验。目标图实际人数、质量、分歧、簇数、采集采用和difficulty同义评论不进入纯图片模型。总体／场景／具体特质／简单反馈／深层表示逐级比较。训练内标准化、降维、选层与调参，外层留房／楼；同覆盖配对与全部覆盖并报。重排、六面、旋转及多人组合不增加独立图片。历史已反复探索，不能把新留出分数称为独立确认性验证。

优先回答：哪些图片特质对应较早统一、少数解释重复支持、较晚发现稀有模式和末段持续变化？关系是否受人数、人员或单个楼驱动？换物理房间是否保持？哪些情况模型仅表达自己的偏差？每条主要发现给出支持、反例、分母与限制。

四、交付。

交付中文报告、逐图结果、同覆盖比较、失败表、运行命令和必要测试；给出候选版／审核版结论变化表。需要本地看图的项目给image_id、实际canonical作答、位置及具体问题。不得替用户填写人工审核结果。无需等待审核完才完成现有候选分析；缺审核时明确留下后续更新入口。

不要修改正式方法合同、SOP资格、原始导出或采集安排；不上传原图、权重、检查截图或ZIP，不创建新的云端任务。最终总结应围绕图片与不确定性、收敛及人员差异的联系，不能以重新命名论文主问题代替分析。
