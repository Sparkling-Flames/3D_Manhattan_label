# 本地前置核查与交付状态

## 已执行

同一有效点快照上完成球面/图上局部最大端点距离 × 完整链接/真实代表半径四组比较，分别保存自动对应及完整人工对应的后见视图。输入仍为2501份、214图、239条件单元；保留2388份，2381份角色分开可计算，2343份唯一自动绑定。绑定组有238个可计算单元，剩余单元仍在全历史验收表，不作为零距离或单人簇冒充结果。

收到RC2的数值与文本材料已保留；16图审核原件与当时审核绑定完全相符，原文未改。uNb-21的最新完整点对映射及相近判断独立保存，yq-15人员澄清不覆盖旧文字。原点、时间和既有排除不变。

## 初步对照，不是视觉准确率

下表使用同一2343份自动绑定池、球面9°/图上25.6像素探针。

|距离/分区|总组数|单人组|容差内却分开的作答对|同组但超过容差的作答对|
|---|---:|---:|---:|---:|
|球面/完整链接|948|601|2068|0|
|球面/代表半径|869|579|98|747|
|图上/完整链接|981|619|2351|0|
|图上/代表半径|896|605|103|791|

这不是“代表半径准确率更高”的证据。完整链接要求两两接近；代表半径要求接近代表，可能让两端成员彼此较远。图上距离并没有自动解决拆簇，在这些探针下更严格。需要结合用户已确认的局部差异和整簇检查选择，不能按簇少或曲线早稳定决定。

uNb-21固定顺序为32.500°/92.567像素；采用完整确认对应后为4.593°/14.436像素。这里变化来自对应，未移动任何点，不据此设统一容差。

与RC2共13341个自动绑定作答对的球面固定顺序距离核对，最大差约1e-11度。以RC2原行序重新分簇，成员关系完全复现。本轮固定canonical身份顺序以保证输入行序不影响结果；yq-26的并列合并因此在6/9/12°下分别改变22/69/90对同簇关系，独立列于RC2_COMPARISON.json。不是距离改变，也不认为新选择语义更正确；该图追加后续争议队列。

## 图上距离与3D

当前球面距离比较单位方向射线，无IoU、面积或重建深度权重。1024×512坐标中竖直移动5像素，无论top/bottom都是约1.758°；相同水平像素偏移的球面角距随纬度变化。

原始水平地面投影的相对距离为cot(bottom俯角)。5像素偏移在5°和30°俯角时分别使重建半径变化约26.2%和6.7%。这支持把3D敏感性作为诊断，但不能把它当当前分簇错误的已知原因。top y只影响高度的判断须固定其余输入；联合曼哈顿拟合不满足这个前提。本轮不以拟合几何替换原点。

## 界面、验收与剩余工作

独立12图页面复用Studio foundation；原16图文件和旧页面保留。方法切换会同时更新指定对/阻挡成员距离及整簇；配对歧义明确显示不可计算。已检查X7-13图上代表组显示、一键全选/取消及S9-12绑定歧义提示，浏览器无JavaScript错误。该检查是界面验收及指定作答查看，不是新分簇全部视觉验收。未保存临时截图文件。

全历史239单元清单的新方法整簇状态全部保持待验收；工作方法确定后逐图完成全部214张图，不能只检查告警图。原图、指定作答、整簇、方法版本和用户最终确认分别记录，旧证据不自动通过新方法。

已通过12项相关测试（Python按仓库要求使用UTF-8模式）。一次未加UTF-8参数的旧入口测试因Windows默认GBK读取旧JSON失败；按既有运行约定加`-X utf8`后通过，未改冻结旧代码或数据来掩盖环境要求。新测试覆盖接缝、top/bottom竖直位移、两种分区保证、输入行序与完整人工映射。成对未覆盖不随分组规则变化的全量数值检查通过。

云端包在仓库外临时目录解压，清空PYTHONPATH后执行同一命令：10份数值/清单输出内容一致（压缩CSV按解压内容比较，gzip时间头不作为数值差异），源版本、审核及代码绑定一致。首次隔离复算暴露旧清单依赖11张历史展示JPG；现数值入口明确不校验这两处历史展示目录的JPG，其他输入/输出完整校验，视觉生成入口仍严格要求全部图片。相应测试确认缺失数值JSON仍拒绝运行，未伪造占位图或修改原清单。

文档索引及项目地图已同步，旧Pro说明标为历史。本轮无Label Studio运营、派发或正式Paper A方法修改，因此未运行这些无关生产流程；正式合同与冻结原始输入保持未改。

新Pro仍需独立数值复核、前k人重聚回放、历史人员分类回接及影响报告；本地收到后复算再审图。目前没有最终方法或容差，没有全量视觉通过结论。正式Paper A合同及采集安排不变。
