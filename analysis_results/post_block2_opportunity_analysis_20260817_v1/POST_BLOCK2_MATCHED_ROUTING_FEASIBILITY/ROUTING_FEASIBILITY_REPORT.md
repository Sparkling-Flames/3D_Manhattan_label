# Matched Routing Feasibility

分析角色：`development_cross_fitted_retrospective_audit_opportunity_estimate`。对 caliper/probability grid 做了完整 graph 与 maximum matching sensitivity；中央网格使用 1000 次固定 seed profile perturbation。

历史数据没有随机化的 next-worker counterfactual，因此不报告 routing effect，也不声称成本可识别。LOBO/leave-family profile covariance 未冻结，相关稳定性逐项标为 not_evaluable。
