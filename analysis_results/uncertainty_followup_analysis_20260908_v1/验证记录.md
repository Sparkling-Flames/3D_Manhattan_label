# 验证记录（2026-09-08）

## 用户确认后的补充验证

- 用户确认三份完整底点顺序，V07缺少11的首条回复已被其完整补充替代；V33历史版本身份确认已保存。原始点不移动、不删除，GT源文件不改写。
- `--confirmed-analysis`执行成功，三份派生布局均通过地面有效性及相机包含检查；旧墙带均因非单值边界不适用。已实际查看3份新3D、3份原图连线、3份相同点集的邻接对照、3份模型对照及分布恢复图；不以贴图拉伸否定几何。
- 同51单元的精确TV、遗漏份额、簇覆盖，以及固定34单元名单混合生成成功；building留图分析同时保留未分层44图/8building与同阶段条件21图/5building，结果不等同外部验证。
- 相同三个测试文件重新执行，**16项通过**。新增测试检查完整底点列表、倒置的上下点索引、单池分布与双池组合对穷举的吻合，以及有效非星形布局与旧墙带的适用性边界。
- 旧待确认入口已保存用户顺序；本地搜索再次生成时会保留该确认，不将用户回复重置为空。旧人工语义表中的最终质量裁决仍为空，点序确认与质量定稿分开。
- 地图和README沿用同一分析包入口，增加最新深度分析链接；没有新增正式协议入口、模型权重、依赖或云端发布。

## 确认前的验证轨迹

- 主生成命令完整执行成功：2501条canonical响应、214张历史图、26人、270单元；四种原读取真人均值复现误差均小于1e-15，缺失位置一致。
- 本地导出搜索执行成功：55份JSON、六个目标图像、170条含重复快照的匹配记录。C1 Semi task3388/ann6154按图像、项目、task、作者及annotation完整键唯一连接到底座，不新增样本。
- 定向pytest执行成功：`test_analyze_uncertainty_handoff.py`、`test_uncertainty_handoff_errata.py`、`test_reconcile_human_review_comments.py`，合计13项通过。
- 新测试覆盖原始坐标不被配对函数修改、不可配对点不被丢弃、端点角色未被“修复”、固定簇覆盖与小样本穷举一致、本地响应／两份GT的原始keypoint ID和点序保留、用户顺序为空、不可评价簇数占位处理及固定51单元分母。
- 实际查看了多视角检索联系图、V01/V33对应另视角、V06近地平线点位、V25/V32关联视角，以及18份针对已有问题的读取敏感性预览。联系图用于定位来源，不能算作新增26图完整人工审查。针对两份明显畸形当前GT的点编号图已查看；另外三份旧阶段响应的点位对照已查看。两幅统计图已查看，标签和分母与表格一致。
- 没有修改正式protocol、SOP、eligibility或统计主线结局；这里的相关与固定旧簇覆盖明确标为探索性。未运行无关Panorama Studio浏览器检查或全仓库测试，因为未修改相应代码。
- 项目地图、文档索引各新增一条本分析入口，保留其他任务已有修改。原导出、原人工记录及旧结果包未被本脚本写入。没有提交或推送。

尚未完成的是人工作者意图和几何裁决：V07/V31待用户指定顺序，V33旧C1响应是否就是用户记忆版本尚未认定，其他15项的限制详见《点序与来源复核》。这些不影响报告中不依赖其真值身份的描述统计，也不能用统计成功替代这些判断。

`FILE_MANIFEST.json`在生成命令末尾列出实际输出文件、大小和CSV列名，不包含自身。报告文字及视觉解释不是由测试自动证实；测试只验证相应计算与数据连接边界。
