# Visualize_output.ipynb 重新组织方案

> **目标**: 将当前notebook从"工程日志"重构为"论文级分析代码"  
> **原则**: 按论文逻辑组织（RQ驱动），而非按开发时间顺序  
> **预期效果**: 审稿人/复现者能按Section 1→2→3→4→5顺序执行，直接得到论文Figure/Table

---

## 📋 新结构概览（9个Section + Appendix）

```
Section 0: Setup & Data Loading
Section 1: Data Cleaning & Definitions
Section 2: Transparency & Data Hygiene (Table 1, Fig 1-2)
Section 3: RQ1 - Efficiency Analysis (Fig 3-4)  
Section 4: RQ2 - Quality & Reliability (Fig 5-6)
Section 5: RQ3 - Expert Identification & Task Assignment (Fig 7-8)
Section 6: Sensitivity Analysis (P0-1 Comparison)
Section 7: Stratification Tables (Table A/B)
Appendix: Diagnostic Figures (Fig S1-S5)
Reference: Column Dictionary
```

---

## 🔄 单元格重排详细方案

### Section 0: Setup & Data Loading (4个单元格)

**目的**: 环境准备、依赖导入、数据加载  
**移动顺序**:

1. **[保留] Markdown: 简短说明** (新写)
   ```markdown
   # HoHoNet Semi-Auto Annotation Quality Analysis
   
   **Purpose**: Support paper RQ1/RQ2/RQ3 with reproducible analysis  
   **Data**: quality_report_20260118.csv (generated by tools/analyze_quality.py)  
   **Structure**: Organized by Research Questions (see paper outline)
   ```

2. **[移动] Cell #2**: 导入库
3. **[新增] Cell: Configuration**
   ```python
   CONFIG = {
       'csv_path': Path('../analysis_results/quality_report_20260118.csv'),
       'random_seed': 42,
       'output_dir': Path('../paper_figures/'),
       'bootstrap_n': 1000
   }
   np.random.seed(CONFIG['random_seed'])
   ```

4. **[移动] Cell #3**: 数据加载 (改为用CONFIG['csv_path'])
5. **[移动] Cell #4-5**: df_raw.shape, df_raw.info()

---

### Section 1: Data Cleaning & Definitions (3个单元格)

**目的**: 清洗数据，定义三个口径集合  
**移动顺序**:

1. **[移动] Markdown Cell #6**: 数据清洗说明
2. **[移动] Cell #7**: clean_quality_df() 函数定义 + 执行清洗
3. **[新增] Markdown: 口径说明**
   ```markdown
   ## Three Analysis Scopes (固定口径)
   
   - `df_transparent`: 全量数据，透明披露用
   - `df_main`: In-scope + layout_used=True，主结果用
   - `df_rel`: 多标注 + reliability_used=True，可靠性用
   
   后续所有图表必须注明使用哪个口径，避免混淆。
   ```

---

### Section 2: Transparency & Data Hygiene (6-8个单元格)

**目的**: 回答"数据是否可靠？门控是否有偏差？"（对应论文Section 4）  
**移动顺序**:

1. **[新增] Markdown**: Section 2 标题
2. **[保留] Markdown Cell #11**: Table 1说明
3. **[新增实现] Cell**: Table 1代码（字段缺失率统计）
4. **[保留] Markdown Cell #17**: Fig 1说明
5. **[新增实现] Cell**: Fig 1代码（Scope/OOS分布）
6. **[移动] Markdown Cell #10**: Fig 2说明（覆盖率与门控）
7. **[移动] Cell #9**: Fig 2代码（图0原代码）
8. **[可选] 附录图S2**: 门控原因详细分布

---

### Section 3: RQ1 - Efficiency Analysis (4-6个单元格)

**目的**: 回答"Semi是否省时？省多少？"（论文核心RQ）  
**移动顺序**:

1. **[新增] Markdown**: Section 3标题 + RQ1简述
2. **[保留] Markdown Cell #13**: Fig 3说明（Manual vs Semi active_time）
3. **[新增实现] Cell**: Fig 3代码
   - Manual vs Semi的box/violin plot
   - 统计检验（Mann-Whitney U + effect size）
   - Bootstrap CI
4. **[保留] Markdown Cell #23**: Fig 4说明（active_time vs 改动幅度）
5. **[新增实现] Cell**: Fig 4代码
   - Scatter: x=active_time(log), y=IoU_edit/BoundaryRMSE/Δn
   - 分层：按condition + Δn_pairs bins
   - Spearman correlation

---

### Section 4: RQ2 - Quality & Reliability (6-8个单元格)

**目的**: 回答"Semi是否影响质量？可靠性如何？"  
**移动顺序**:

1. **[新增] Markdown**: Section 4标题 + RQ2简述
2. **[保留] Markdown Cell #33**: Fig 5A/B说明（IAA + LOO共识）
3. **[新增实现] Cell**: Fig 5A代码（IAA分布）
   - df_rel上iou_to_others_median的分布
   - 按condition分层对比
   - 统计检验
4. **[新增实现] Cell**: Fig 5B代码（个体 vs LOO共识）
   - df_rel上iou_to_consensus_loo vs iou_to_others_median
   - Scatter或并列box
5. **[可选] 新增**: 改动幅度 vs 质量的scatter（证明两者可分离）
6. **[可选] 附录图**: 按annotator的IAA汇总

---

### Section 5: RQ3 - Expert Identification & Task Assignment (4-6个单元格)

**目的**: 回答"能否通过$r_u$优化任务分配？"  
**移动顺序**:

1. **[新增] Markdown**: Section 5标题 + RQ3简述
2. **[新增实现] Markdown + Cell**: $r_u$估计
   - 从df_rel聚合per-annotator的median(iou_to_consensus_loo)
   - Bootstrap CI
   - 表格展示
3. **[新增实现] Cell**: $r_u$ vs active_time关系
   - Scatter: x=r_u, y=median(active_time)
   - 验证"高r_u是否需要更多时间"
4. **[保留] Markdown Cell #29**: Fig 7说明（任务×标注者热力图）
5. **[新增实现] Cell**: Fig 7代码
6. **[新增实现] Cell**: 分配策略对比（如有验证集数据）

---

### Section 6: Sensitivity Analysis - P0-1 Comparison (3-4个单元格)

**目的**: 证明P0-1修正的必要性和稳定性  
**移动顺序**:

1. **[新增] Markdown**: Section 6标题 + P0-1简述
2. **[新增实现] Cell**: 模拟旧逻辑（保留n_pairs_mismatch gate）
   ```python
   # 在df_clean上模拟旧逻辑
   df_old_logic = df_clean.copy()
   df_old_logic['layout_used_OLD'] = df_old_logic['layout_used_clean'] & (
       df_old_logic['pred_n_pairs'] == df_old_logic['ann_n_pairs']
   )
   ```
3. **[新增实现] Cell**: P0-1前后对比表
   - 覆盖率对比（67% vs 98%+）
   - RQ1结论是否稳定（Manual vs Semi省时效果）
   - RQ2结论是否稳定（IAA分布）
4. **[新增] Markdown**: 敏感性分析结论总结

---

### Section 7: Stratification Tables (3-4个单元格)

**目的**: 生成论文Table A/B（按Δn和difficulty分层）  
**移动顺序**:

1. **[新增] Markdown**: Section 7标题
2. **[新增实现] Cell**: Table A（按|Δn_pairs|分层）
   - 计算delta_n_pairs
   - 分bin: Δn=0, ±1, ±2, >2
   - 统计：Coverage, 2D IoU, 3D IoU, Active Time
3. **[新增实现] Cell**: Table B（按difficulty/model_issue分层）
   - 效率（active_time）
   - 一致性（IAA, r_u）
4. **[可选]** Table C: 按annotator的汇总

---

### Appendix: Diagnostic Figures (10+个单元格)

**目的**: 放不适合主文但需要的诊断图  
**移动顺序**:

1. **[新增] Markdown**: Appendix标题
2. **[保留] Markdown Cell #15**: Fig S1说明（active_time分布）
3. **[新增实现] Cell**: Fig S1代码
4. **[保留] Markdown Cell #19**: Fig S2说明（门控原因详细）
5. **[新增实现] Cell**: Fig S2代码
6. **[保留] Markdown Cell #21**: Fig S3说明（指标分布）
7. **[新增实现] Cell**: Fig S3代码
8. **[保留] Markdown Cell #25**: Fig S4说明（标注者间指标差异）
9. **[新增实现] Cell**: Fig S4代码
10. **[保留] Markdown Cell #27**: Fig S5说明（标注者耗时差异）
11. **[新增实现] Cell**: Fig S5代码
12. **[保留] Markdown Cell #31**: Mixed-scope任务说明
13. **[新增实现] Cell**: Mixed-scope任务代码
14. **[保留] Markdown Cell #37**: 反例表说明
15. **[新增实现] Cell**: 反例表代码

---

### Reference: Column Dictionary (2个单元格)

**目的**: 作为字段说明文档，便于查阅  
**移动顺序**:

1. **[移动] Markdown Cell #39**: 字段说明（完整版）
2. **[可选] Cell**: 打印列名/示例

---

## 🎯 重排后的预期效果

### 对审稿人：
- ✅ 清晰的Section结构，直接对应论文RQ
- ✅ 每个Figure都有统计检验，不是"画了就算"
- ✅ P0-1敏感性分析独立成节，修正必要性一目了然

### 对复现者：
- ✅ 按顺序执行即可得到论文所有Figure/Table
- ✅ CONFIG集中管理路径/参数，不用到处找硬编码
- ✅ 每个Section独立，可单独调试

### 对你自己：
- ✅ 论文写作时直接引用对应Section的Figure编号
- ✅ 修改分析策略时只改对应Section，不影响其他
- ✅ 补充RQ3分析时，Section 5是空白模板，填入即可

---

## ⚠️ 重排时的注意事项

1. **保持Cell ID**: 重排时不要删除原Cell ID（除非是新增Cell），方便diff对比
2. **测试执行**: 重排后从头到尾Run All，确保没有依赖断裂
3. **Git提交**: 重排前先commit当前版本，万一出错可以回退
4. **文档同步**: 重排后更新README，说明新结构

---

## 🚀 重排实施步骤（建议手动操作）

由于Notebook的Cell ID和依赖关系复杂，建议手动操作：

1. **备份**: 复制当前notebook为 `visualize_output.backup_before_reorganize.ipynb`
2. **创建新结构**: 在VS Code中打开notebook，按上述方案移动单元格
3. **补充TODO**: 把所有"TODO"标记的单元格改为实际实现
4. **测试**: Run All，检查是否有报错
5. **验证**: 检查df_transparent/df_main/df_rel是否正确生成
6. **提交**: Git commit -m "Reorganize notebook by RQ structure"

---

## 📝 重排后立即要做的5件事

1. ✅ **补齐RQ2图表**: Fig 5A/B（IAA + LOO共识）
2. ✅ **补齐RQ3框架**: $r_u$估计 + 任务分配对比
3. ✅ **添加统计检验**: 每个对比都要显著性标注
4. ✅ **实现P0-1对比**: Section 6的敏感性分析
5. ✅ **生成分层表**: Table A/B（Δn + difficulty）

预计工作量: **2-3天**（如果全职做）
