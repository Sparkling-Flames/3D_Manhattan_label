# 分簇工作版：统一数据入口与验收

**2026-09-21结构增减规则再次明确：**用户确认“多画了/少画了结构”就是全景布局的不同看法，应分簇；奇数点按既有流程单独复核，不再将该原则列为待讨论问题。[本轮24图筛查](../../analysis_results/cluster_screen_20260921/README.md)复用Studio核对同点数定位、对应和整簇一致性，答案留空，避开已列明的既有审核图片。没有取消点数硬门、自动修改阈值或改变正式资格。

**2026-09-21用户终审入口：**运行`python -X utf8 -B -m tools.thesis_main.analysis.reviewed_manual_20260921`，结果见[审核后汇总](../../analysis_results/new_manual_reviewed_20260921/汇总说明.md)。本批48份父关联均确认独立，问题关闭；已落实两份补点、7272版本和三份上下对应。原导出保留，raw/effective点与借用来源分开保存。下文旧入口及人数保持历史版本，不代表新增终审后的数据。 W006补交原分配的uNb-66／67／87已接入，原480份必做均已有非空提交并纳入；7502替代7111，不重复计票。

本页记录探索研究工作版，不修改正式 Paper A 方法合同。RC1分开top/bottom、环序最大角误差、完整链接9°保留为旧工作基线，不是最终推荐。当前按用户选择，以全景图局部点位为主、3D为辅助，比较局部距离和整簇规则；见[新Pro任务](Pro新对话_局部点位分簇与影响核查_20260920.md)与[独立实验](../../analysis_results/local_point_clustering_20260920/README.md)。容差未冻结。

## 数据与人工点处理

入口为 `python -X utf8 -B -m tools.thesis_main.analysis.clustering_release`。历史 2,501 份作答逐份回查了18份原导出；W019/W026共113份沿用原排除。Project28的4738只更正坐标，4784删去越界点，身份和独立人数不变。当前几何主候选可计算2,381份，另7份不可计算。历史冻结时间原样保留，研究者改点不新增作答时间。

**计算使用人工处理后的有效点集，不以原始奇数点数排除。** 原始奇数点中11份已确认补删：7份进入当前主候选（2份补点、5份删点）；另外4份属于既有排除人员W019。逐份审计见运行结果的 `CONFIRMED_ODD_REPAIR_AUDIT.json`。原点、有效点、操作及确认来源同时保留。补点不冒充标注员原答；借用他人信息的补点在独立预测中另做不借用信息的敏感性分析。

未确认奇数点不能自动补删。分开 top/bottom 的方法可条件性比较角色数量不等的点集，不表示已经完成人工复原；绑定点对方法另有资格条件。不得把两者可计算人数混用。

## 运行、接入与版本

每次运行绑定输入、审核原文、冻结时间、方法配置和代码。结果另有清单；审核页核对输入及结果清单后才生成。发生版本变化须重新运行，旧点号裁决不得直接套用。

已有运行复算：

```powershell
python -X utf8 -B -m tools.thesis_main.analysis.clustering_release --run-root analysis_results/clustering_release_local_20260920/current
```

新批次先创建独立目录，使用 `--prepare --project28 <已确认新导出> --new-batch-manifest <显式身份映射JSON>`。映射必须提供 batch_id、stage、project_id、export_path、worker_map、tasks、record_type=independent_initial、annotation_form_version=manual_scope_only_v1；每个任务提供 runtime_task_id、image_id、building_id、raw_condition=manual、assistance_exposure=none。未知人员、图像不符、同人同图同条件重复、同身份改点均停止，待人工解释修订关系。未采集Difficulty/Model Issue保持未采集／不适用。

当前新增入口的时间统一为未冻结，不以 lead_time 填补；收到真实新日志后须经过现有时间审计再接入经确认的冻结版本。本轮只验证软件接入，不能称为实际新批次验收。480份为计划量，完成进度未核实。

## 视觉与云端边界

最新数值包已[接入并本地审查](../../analysis_results/clustering_numeric_received_20260920/README.md)：200条共享真人顺序逐前缀重聚、固定人员子池、人员迁移与真实四人组合均有结果。独立复算证据位于[本地验收记录](../../analysis_results/clustering_numeric_local_20260920/README.md)。这是固定历史版本的研究入口，不替代上面的新批次入口。冻结时间及原始点不变；人工确认补删后的有效点继续参与计算，借用补点另做训练/评价隔离。

图上代表半径与图上完整链接分别反映“接近代表”及“组内两两接近”，均仍待人工验收。每个方法同时保留成对覆盖、整簇直径和人员归属变化，不能用组数减少替代覆盖改善。只替换一对人工对应的后见距离表可能不满足三角不等式，必须显式标记；不据此自动传播裁决或改动原点。

首批16图用户审核已接收并核对绑定，原文、空选项及暂缓原样保留。新增12图页面仅处理暂缓与既有争议，复用示范foundation且独立保存新答案；已确认的相近意见作为只读证据。确认相似不强制合并整簇，部分对应不补全。uNb-21最新完整点对映射进入单独的后见敏感性视图，不改原点，不作统一容差或推广至他人的依据。

**最终视觉范围是全部历史标注图片，当前为214图、239图片×条件单元。**方法确定后逐图检查所有适用分簇，并明确记录原图、指定作答、整簇、方法版本和人员覆盖。先审争议不等于只审争议；未审单元保持待验收，无自动告警不代表通过。旧39图等开发证据不能称独立验证集。

云端只做数值链条独立核查、历史人员研究回接及分析示范，不作视觉裁决。人员分类不固定四类、不因单人簇多追加排除；分类与目标评价隔离。OOS、遗漏、差错及少数表达保留研究价值。

发布仍须人工审核和真实新批次核验。数值门通过不等于分簇正确率已经验收；缺失、未决和未冻结分别报告。
