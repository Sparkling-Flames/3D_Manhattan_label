# 历史作答难度粗分类与图片联系：Pro续研任务｜2026-09-15

## 当前状态与后续执行（用户最新确认）

早期候选为 **k=2—8（含8）**；此前k<8已被用户后续授权替代。两轮Pro研究均由用户要求开展，模式发现、逐簇复现、支持核心稳定属于获授权的探索，不因与旧任务不同而判为偏离。

第一轮历史粗分类及A—E、第二轮中等／困难诊断均已接入本地，原有60项测试通过；尚未按新类重拟合A—E。继续以两版候选和连续过程量推进图片联系研究，不等待冻结最终收敛标准。**粗分类须经用户审核：算法候选 → 本地原图／作答证据 → 用户审核 → 审核版分析。**算法原值、审核结果、原106张tag分别保存；未审核和暂缓不等于困难。审核前结果标为候选探索，审核后只重算受影响部分。最新[执行状态与提示词](../../analysis_results/image_portrait_20260914_v1/review_workflow_20260915/README.md)另记录9图OOS任务仍含无辅助几何的覆盖问题；下面保留第一轮任务内容并同步已变更的k范围。

**本轮目标：从历史真实标注中重新派生图片难度粗分类，扩大具有作答证据的研究覆盖，再分析这些类别与648图既有场景分类、可见特质及模型表示的联系。**粗分类是研究工具，后续图片关联与预测必须实际完成，不能只交付分类方法和阈值讨论。

用户明确停用旧实验的difficulty标签。旧标签及其同义重编码不再作为本轮目标、特征、分组依据、阈值选择依据或验证标准；原始文件保留，不删除、不改写。历史真实作答、图像分类和来源明确的冻结视觉模型特征仍可使用。用户随后明确确认：**只停用旧实验字段，保留106张人工tag作独立对照。**这批人工tag来自`latest_selection_record.difficulty`及其已核对的用户原记录，不能因字段同名而误删。粗分类不依赖它们生成或调参，两套标签在重叠图上比较一致与不一致，并允许分别研究图片关联，不合并为同口径真值。

这次任务接续[主空间与历史粗分层逻辑复核](主空间与历史粗分层逻辑复核_20260915.md)。该复核中“继续优先以106标签为目标”的建议不再作为下一轮主任务；其关于收益集中、同房信息条件与S9机制解释的核查仍有效。正式方法合同、采集安排及旧实验结果不因此修改。

用户的三档直觉保留为以下**待检验定义**：

|粗类|希望描述的现象|必须保留的限制|
|---|---|---|
|简单|少数人后形成统一共识，或仅有两个稳定且获重复支持的簇|早期k逐一比较2、3、4、5、6、7、8；同时检查比例与几何变化，不能只有最终簇数。|
|中等|十几人范围内稳定为少数x簇，单人簇少|x可比较2、3、4；时间、支持和单人比例阈值均标明版本，不要求凑出固定比例的中等图。|
|困难候选|较多簇、较多单人簇，且在已有观察内仍有结构、比例或定位变化|记录“观察内未稳定”；仅凭多簇／单人簇不能推出无法收敛。稳定多簇继续允许稳定。|

这里“单人簇”指仅一名真实人员支持，区别于“一个支持主簇”。未知、无效、未观察到足够人数和三档不适用的轨迹是状态字段，不强行增设一套六档主分类，也不硬塞进困难类。质量与分布稳定性分别保留，统一作答不自动意味着标注正确。

以下整段可直接交给已完成`mainspace_history_readable`的Pro。它是后续研究指令，不表示新实验已执行。

```text
请接着上轮mainspace_history研究独立开展数值分析，并实际完成结果。用户的主线是不确定性及其随真实人数增长的收敛过程；当前希望用历史标注形成有证据的图片难度粗分类，以扩大仅106张人工难易tag之外的研究覆盖，然后继续研究图片分类、图片特质、模型表征与这些粗类的联系。

一、输入与本轮明确停用的内容

沿用HOHONET仓库codex/image-portrait-20260914分支及analysis_results/image_portrait_20260914_v1/。先核对AGENTS.md、最新README、评价口径、本轮任务和主空间逻辑复核。输入可来自已有仓库和你上一轮实际交付；登记实际来源，不能把本地路径或目录名称当作云端已收到文件的证据。

旧实验自带的difficulty标签全部停用，不读取它来定义新类别、训练预测、验证效果或选择参数，也不通过risk等级、文字同义词等方式偷偷接回。只登记排除的字段来源即可，不继续分析旧标签。历史真实作答及图像分类仍使用，模型特征按实际来源判断，不能仅因名称含“旧”就一并删除。

用户已明确保留这次亲填的106张人工tag作独立对照：简单49、中等41、困难16，来源是metadata/spatial_history.jsonl.gz的latest_selection_record.difficulty及已核对的用户审查原记录。不要按字段名“一刀切”排除这批人工tag。新历史粗类形成后，比较重叠图片上的一致与不一致，以及哪些图片特质分别关联两套目标；不合并标签、不覆盖原值、不拿人工tag反向挑历史粗分类阈值。只在各自任务中训练和评价，不能把同一图的两种标签当作两张独立图片。上一轮重叠35张只是待复核覆盖，不是新增标签必然等于205减35。

上一轮历史过程有205个独立image_id、230个图×条件单位；这只是待核对库存，不是必须凑出的数量。重新报告648图分类池、历史作答、有效几何及模型特征的交集。不能继续把特征输入限死在上一轮106张：尤其要检查新增历史目标图是否实际读到DINO等数组；648运行状态不等于648数组均已取得。

保留W011历史，W019/W026排除当前主分析。Manual作为无辅助主分析，Semi按实际初始化和条件另报；不同条件不合并为一个图片标签。点数不同必须分簇，每个已支持簇至少2名不同真实人员，人工确认的补点／删点沿用记录，合理单人作答保留。无效、失败、OOS与分布变化分开，不能删除失败后冒称全员有效共识。

二、先落实历史作答粗分类，不只讨论定义

以用户的三档直觉为出发点，先产出一版可追溯的历史粗分类表，再以少量有理由的替代定义检查敏感性。

简单：少数人已形成统一共识，或仅有稳定、重复支持的两个簇。早期k逐一比较2、3、4、5、6、7、8，含8是用户最新授权。需要同时记录模式结构、份额变化和簇内变化，不能用最终簇数替代过程。

中等：在十几人观察范围内达到稳定的少数x簇，单人簇较少。x先比较2、3、4，较晚窗口可沿用10、12、15、18、19。用实际人数判断已观察到什么；没有观察至某个窗口不能伪造该窗口的状态，也不机械要求所有图补到相同人数。

困难候选：较多簇、较多单人簇，且观察末段仍存在结构、比例或几何变化。为“较多”和“较少”给出少量可解释候选，并报告数量和占真实人数的比例。多簇稳定、单人簇较多但轨迹已稳定等例外必须保留，不得自动称为无法收敛。最终措辞为“在当前观察范围内未稳定／困难候选”，不能推断增加任何人数也不会稳定。

保留三档输出和独立的判定状态：证据不足、定义间不一致、无效及不适用轨迹，不把这些状态硬变成第四个有序难度等级。保留连续过程量，使分类边界附近的图片仍可分析。若某档为空，解释是人数覆盖、支持条件、窗口、顺序要求还是资料本身造成，不能为了填满类别去选阈值。

特别修正上轮“中等”的解释：P(k<起点≤h)≥q要求大量起点集中在较晚区间，容易漏掉早晚混合但到h累计已稳定的图片。并列检验“先判断早期是否达标；未达标者，再判断累计P(起点≤h)是否达标”的定义。q、模式上限和单人比例阈值须在看图片预测效果前确定候选；报告两种定义的覆盖与变类，不能以哪种更容易被模型预测来挑标签。顺序比例只是有限真实人员池内的重排结果，不是独立重复实验的置信概率。

稳定后缀与固定尾段可以同时报告，但名称与时间范围必须一致。不能把一次很短的观察后缀包装成长时间稳定，也不能要求统一追加5人；早期5人后只观察到6人与早期5人后观察到24人的证据强度分别记录。人数越多越容易看到稀有模式，因此主结果保留实际人数，另在可支持的共同人数下作敏感性比较；不能把统一截断作为主分析，也不能把观察人数当图片难度特征。

三、建立图片与粗类的联系：以下A—E均给出实际结果或具体覆盖不足

A．可解释图片特质。比较大类、功能细类、主呈现空间，再加入边界可见性、遮挡、门洞、连通空间、反射／玻璃、低对比度等有来源的记录。分别保留人工、AI与未知。主空间功能编码不等于具体几何位置；不要把功能分层增益直接解释成门侧或淋浴内部的作用。重点看同类别／同功能内部的难度差异，报告支持和不支持的关系。

B．模型结构反馈。检验预测点数、双头范围差异、旋转敏感性、不同布局模型的分歧，是否与新粗类及其连续过程量有关。先有简单反馈基线，再谈高维表示；既有信息家族含反馈时不能把家族总收益全称为高维特征贡献。区分模型偏差与图片证据，不把模型一致当GT。

C．模型层与表示。比较实际已导出的HoHoNet各阶段／旧均值、Bi Fc与两种Fg、uLayout压缩与上下文表示、DINO第3/6/9/11/12层及CLS、全景／六面／局部／全局、DA3可用表示。继承预指定候选，允许另列有根据的探索，不只展示赢家。上轮106人工tag上的DINO弱结果不能直接替代本轮历史过程目标的检验。DA3几何输出只在有效条件下使用，不把已知不一致的相机结果当可靠物理重建。

D．图片间迁移。同房不同视角、同类不同房间及跨building分别分析。分别报告仅目标图片特征的预测，以及允许使用来源图片历史粗类／过程的条件迁移；后者增加了输入信息。DINO同房近邻应记录实际邻居ID，并区分同房标签全相同和组内有差异的组件，补充一致概率表达的频数／简单近邻对照。房间关系按支持证据，不把展示组自动当物理房间；待定及重叠关系不能跨两侧泄漏。

E．人员构成影响。检查同一图片的粗类会否随真实人员子集明显改变，并区分人数与构成。优先使用共同人员、相同人数的可比检查，保留人员名单和条件；不能把人员差异造成的粗类变化全解释成图片属性。若使用人员分类，必须在目标房间之外形成，沿用既有候选及不分型对照，不在本轮强行冻结分类或重做完整人员分型普查。不得复制人员或为了每个组合重新假定有独立作答。

四、评价与解释

至少比较总体训练侧基线、仅场景类别、主空间功能、具体特质、简单模型反馈、各层表征与必要组合。先用简单近邻与正则化预测，不训练视觉网络。所有标准化、降维、选层和调参只用当前训练侧。固定候选标签规则可逐图计算目标，但由全数据学习的阈值、分区尺度或人员得分须训练侧形成；不得用外层目标调整。

图像预测输入禁止使用目标图的历史分歧、簇数、实际人数、计划人数、质量、时间、difficulty同义文字或采用决定。这些可以用于生成／审查目标和描述覆盖，不能同时作为纯图片预测输入。允许使用其他图的历史结果时必须单列信息条件，目标图的作答仍留出。

遵守现有同房留视角、跨房保守隔离和留楼设计；同图旋转、六面、重排与图×定义都不增加独立样本。同时报告共同覆盖与全部可用覆盖，各类表现、图均与房／楼均结果，避免大组主导。类别定义敏感性与模型不确定性分开。困难若只出现在少数房间或某人数档，应直接报告，而不是包装成普遍规律。

做有用的解释，不只排序模型：哪些可见特质对应容易统一、稳定双簇、较晚少簇、观察内持续变化？哪些仅识别了场景类别或人员／采集背景？能否在另一房间复现？必要时使用连续目标分析，避免所有信息被三档切分吃掉；但不得以连续目标替代用户要的粗分类结果。

本地已经发现S9hNv5qa7GM_bd9faec23bb3462c94a5fbc6c0a3d5cf的100条重排中，G10与D10起点完全相同。它证明最终单一支持主簇也可能较晚满足规则，却不能单凭G10名称归因于簇内定位。后续机制解释应核对逐步变化由哪个条件触发。

五、交付

只分析数值、几何和来源文字，不查看原图、下载权重或调用视觉模型。需要看图的内容给出image_id、配对成员、具体问题和数值触发依据，由本地处理。

输出独立cloud/history_difficulty_20260915_v1/，保留上一轮结果。至少交付：历史派生粗标签逐图表及人数／人员／条件／定义版本；参数敏感性和类别变化；A—E结果与缺口；固定划分逐图预测和各层同覆盖比较；关键反例及审图清单；中文解释、代码、运行命令及必要边界测试。标签表建议使用独立字段history_difficulty_tier，不能覆盖任何原difficulty字段。明确哪些字段是新派生、哪些是模型预测、哪些无法判断。

无历史作答的图片保留在648图画像池；不能补造真实粗类。模型对它们的输出只能叫待验证预测，不扩大真实标签样本数。已有缓存足够的分析先完成，输入不足的路线列出具体缺项；不要只再交一份计划，也不要停在近百万条定义网格而不分析图片联系。

最终回答：这次真实扩展了多少张可研究图片？三档在哪些定义和人数范围下稳健？哪些图片特质／模型信息与粗类及过程有联系？哪些联系受房间、人员或观察上限驱动？最值得本地看图与后续新增真人验证的具体例子是什么？
```

文档状态：第一轮历史粗分类及A—E已由Pro执行并接入本地；不表示第二轮分类已获用户审核或本地已全量复算。未自动创建／发送云端任务。“旧difficulty停用”只作用于这轮探索消费，不删除历史真源；正式Paper A合同和采集安排不变。
