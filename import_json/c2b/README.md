# C2-B D8 Batch A formal import JSON

- `c2b_D8_batch_a_import_zh.json` 仅用于 175.178/http 中文入口。
- `c2b_D8_batch_a_import_foreign_https.json` 仅用于 HTTPS 海外入口。
- 两份文件包含同一组 46 个 Manual 任务，只允许分别导入对应入口，不得混用。
- `vis_3d` 来自冻结的 HoHoNet layout JSON；未写入 Label Studio `predictions`。
- 文件仅在本地生成，未自动调用 Label Studio API。
