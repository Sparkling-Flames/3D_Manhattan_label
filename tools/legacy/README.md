# tools/legacy

本目录用于存放**已被替代**但为了可追溯性而保留的脚本实现。

- 你应优先使用 `tools/viz_quality_analysis.ipynb`、`tools/viz_quality_utils.py`、`tools/save_quality_figures.py`。
- 为了兼容旧命令行入口，部分脚本在原路径保留了 wrapper（会转发到 legacy 实现）。
