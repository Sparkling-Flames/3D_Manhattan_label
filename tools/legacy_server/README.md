# Legacy server tools

This folder preserves scripts/configs that were used with the old static server workflow.

- `prepare_labelstudio_docker_old_server.py`: original version before COS-oriented update.

Use these files only when reproducing old server behavior.
