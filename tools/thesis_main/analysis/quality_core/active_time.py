"""Active-time parsing boundary.

Owns the future split for active log resolution, JSONL loading, session max,
cross-session sum, match/source fields, and lead-time fallback policy.
"""

BOUNDARY = "active_time"
