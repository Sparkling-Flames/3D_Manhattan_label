# OOS 目录说明

当前 `OOS/` 目录用于保存 OOS gate candidate pool。

## 当前状态

- 当前目录下共 10 个任务
- 当前没有活动中的 `directory bucket != current scope_binary` mismatch
- 但 OOS 的目录 family 与 final scope subtype 仍可能不完全一致
- 当前显式例子是 `task560`：
  - 目录位于 `边界不可判定`
  - 但 latest verified export / final gold 里的 `scope` 是 `oos_geometry`
  - 因此执行层应以 final gold scope subtype 为准，而不是以目录名强行覆盖

## 当前角色边界

- 这里的任务用于 `scope / gate` 能力审计
- 它们不是 manual geometry anchor
- 也不是 semi trap 的默认几何主池
- 当前仍然只是 OOS candidate pool，不等于 OOS final quota / binding

## 当前使用提醒

1. 是否属于 OOS，以 latest export 里的 `scope` 为主，不以旧目录位置单独决定。
2. low-priority 的 OOS 样本含义是“是否算 OOS 仍较不确定”，不是“无法标注”。
3. low-priority OOS 样本应继续保留在 registry 里，但不应自动进入 final selection。
4. 若目录 family 与 final scope subtype 不一致，导入层与 binding 层应以 final gold 的 `scope_target` 为准。
5. 当前 OOS truth layer 与 final gate 都已可抽取、可审计；当前主 gate 与 audit-only 的分流以 [oos_final_quota_binding_v2.json](d:/Work/HOHONET/analysis_results/phase1_progress_20260324/oos_final_quota_binding_v2.json) 为准。
