当前这个目录仍应视为 `manual` 里的困难子集，但要区分两个样本：

- `696(低优先,不确定,难标注)`：继续按低优先处理，不进入人工锚点集
- `711(中低优先,难标注)`：当前作为保留例外，不按普通 `低优先` 自动排除；如果遮罩 family 只保留一个可操作样本，优先级是 `711 > 696`

当前最稳的理解是：

- 这两个样本都不适合直接当默认的 expert-anchor 主池
- `696` 继续保留为更低优先的困难样本
- `711` 可以保留在 manual pool 中，作为遮罩 family 的较优代表与复核样本

所以这个目录当前最准确的定位是：

> **保留，但分层使用：`711` 保留，`696` 继续低优先。**
