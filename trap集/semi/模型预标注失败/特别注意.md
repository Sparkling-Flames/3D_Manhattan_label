当前这个目录只保留 `task475(低优先)`，而且它仍然不应被理解成默认的 prescreen core trap family。

当前结论：

- 这类样本很难形成稳定的立方体式精标
- 当前更适合作为 `low-priority / optional / audit case`
- 不应默认进入 thesis-facing 的 semi 主 trap 配额
- 如果后续需要展示自然失败样例，可以保留它；如果目标是默认主配额，它应排在后面

所以这个目录当前最准确的定位是：

> **保留作审计或补充案例，不作为默认核心 family。**
